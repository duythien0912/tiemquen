# Workflow report: tiemquen-mvp

- Status: complete
- Duration: 143h 30m 47s (2026-07-10T10:16:41.454Z → 2026-07-16T09:47:28.601Z)
- Phases done: Setup, DoDBaseline, Foundation, Engine, ImportAgent, BuyerOrders, FlyerSeller, Verify, CodeGate, DoDGate
- Branch / worktree: wf/tiemquen-mvp / .worktrees/tiemquen-mvp

## Definition of done — 5/5 MET

| Criterion | Tier | Verdict | Evidence | Baseline |
|---|---|---|---|---|
| unit-tests: Full unit test suite passes (pytest, all modules: a2ui validator/repair, compose, import, order state machine, notify fallback, vietqr CRC, qr batch). | checkable | MET | Ran `./.venv/bin/python -m pytest -q` in /Users/thiennd/Documents/tiemquen/.worktrees/tiemquen-mvp: "257 passed, 1 warning in 10.05s", exit 0. Only warning is an upstream starlette/httpx deprecation notice, not a failure. | error |
| e2e-mock: End-to-end smoke runs WITHOUT any Gemini API key (mock mode): import fixture -> chuẩn menu -> compose valid A2UI -> cache -> place COD order -> state machine created->seller_seen->confirmed->done -> notify fallback fires -> group order split -> QR batch + VietQR payload + flyer PDF generated. | checkable | MET | Ran `env -u GEMINI_API_KEY ./.venv/bin/python scripts/e2e_smoke.py` in the worktree: exit 0, final line "SMOKE OK — all steps passed." Output shows every required stage: compose cache for 4 variants (STEP 9), COD order created→notify chain fired ([fcm-stub]/[console]/SMS fallback lines)→ack→seller_seen→confirmed→delivering→done + 409 on illegal terminal transition (STEP 10), 3-member group order with exact uneven split (STEP 11), A5+A4 QR batches + mock imagen hero PNGs + print-ready flyer PDFs >10KB (STEP 12), VietQR EMVCo TLV payloads with valid CRC16-CCITT (STEP 13). | error |
| artifacts-exist: Core deliverable files exist: buyer renderer, seller PWA entry, menu schema, catalog. | checkable | MET | `test -f buyer/renderer.js && test -f seller/index.html && test -f shared/menu_schema.json && test -f agents/tiemquen_agent/catalogs/tiemquen_emenu_v1.json` in the worktree printed ALL EXIST, exit 0. | unmet |
| buyer-no-llm: Golden rule (ARCH §5.1): no LLM call anywhere on the buyer path — buyer/ static assets and the order-serving endpoints (view menu, place order, order status) must not import or invoke google-genai or any LLM. LLM only in compose/import/flyer/order-parse (compose-time). | judged | MET | grep of buyer/ for genai\|gemini\|llm matches only comments asserting zero-LLM (buyer/renderer.js:6-7, buyer/order.js:2, buyer/context_rules.js:2-4); no code path calls any LLM. grep of infra/ and shared/ for genai\|gemini\|generativeai: zero hits. All google-genai imports are lazy, inside functions of compose-time modules only: agents/tiemquen_agent/imagen.py:105,144, agents/tiemquen_agent/toolable.py:196-197 ("lazy import: real mode only"), agents/tiemquen_agent/agents/import_agent.py:119, compose/composer.py:279. Order-serving routes in agents/tiemquen_agent/server.py — GET /api/shops/{slug}/composed/{variant} (line 387, reads cache only), POST /orders (438), GET /orders/{id} (470), /status (477), /ack (487), /transition (495), group-orders (527-584), GET /t/{slug} (738, serves static buyer/index.html), GET /g/{gid} (747) — invoke only OrderStore/GroupOrderStore/cache/notify, never composer/toolable/imagen. The only LLM-touching endpoints are /api/import, /compose, /hero, /flyers, and /orders/parse-text (order-parse), all explicitly permitted compose-side paths. E2e smoke passing with GEMINI_API_KEY unset corroborates. | — |
| readme-accurate: README quickstart is accurate: a fresh dev can create venv, install, boot the server, open the demo shop buyer page and seller dashboard by following it verbatim. | judged | MET | Replayed README.md quickstart (lines 9-31) verbatim in a fresh git clone of wf/tiemquen-mvp under the scratchpad: venv + `pip install -r requirements.txt` succeeded (Python 3.12.12); `uvicorn agents.tiemquen_agent.server:app --port 8787` booted with no API key; `curl -X POST /api/shops` seeded the demo shop (shop_comtamcoba); `POST /api/shops/com-tam-co-ba/compose` returned 4 variants x 8 messages; buyer page http://127.0.0.1:8787/t/com-tam-co-ba returned 200 containing __TIEMQUEN__ bootstrap + renderer.js reference, seller dashboard /seller/ returned 200 with the PWA HTML, /health returned {"status":"ok"}, and the composed-variant fetch the buyer page performs returned 200; steps 5-6 in the fresh clone: 257 pytest passed and "SMOKE OK — all steps passed." Caveat (environment, not README): this machine's Homebrew python3.14 is broken (pyexpat dlopen: Symbol not found _XML_SetAllocTrackerActivationThreshold vs /usr/lib/libexpat.1.dylib), so a bare `python3 -m venv` fails locally; README already states "Python 3.12+" and the quickstart works verbatim with any healthy interpreter. | — |

## Tokens & estimated cost

| Model | Input | Cache write | Cache read | Output | Est. cost (USD) |
|---|--:|--:|--:|--:|--:|
| claude-sonnet-5 | 1,714 | 6,129,334 | 254,750,346 | 606,996 | $108.52 |
| <synthetic> | 0 | 0 | 0 | 0 | n/a (no rate) |
| claude-fable-5 | 62,740 | 2,654,813 | 41,533,431 | 363,122 | n/a (no rate) |
| **Total** | 64,454 | 8,784,147 | 296,283,777 | 970,118 | **$108.52** |

> Estimate. Token attribution is by time window over transcripts in `/Users/thiennd/.claude/projects/-Users-thiennd-Documents-tiemquen` (a concurrent unrelated session in the same window would inflate it). Rates are editable in this script or via the RATES_JSON env var.
